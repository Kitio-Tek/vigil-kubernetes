apiVersion: pg.athos.io/v1alpha1
kind: PostgresUser
metadata:
  name: e2e-app-user
  namespace: default
spec:
  clusterName: e2e-cluster
  databases:
    - name: postgres
      privileges:
        - SELECT
        - INSERT
        - UPDATE
  connectionLimit: 10
