name: Security

# Runs on the default branch, on PRs into it, and weekly on a schedule. The
# advisory nature of govulncheck means we keep `continue-on-error: true` on
# that job so a fresh stdlib advisory does not page maintainers via the
# default Actions failure email.
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 6 * * 1"
  workflow_dispatch: {}

permissions:
  contents: read
  security-events: write

env:
  GO_VERSION: "1.26.3"

jobs:
  govulncheck:
    name: govulncheck
    runs-on: ubuntu-latest
    # Advisory: govulncheck reports stdlib advisories that often track the
    # runner's Go toolchain rather than this project's source. Surface the
    # findings without failing the run so a fresh stdlib advisory cannot
    # block unrelated changes from merging.
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          check-latest: true
          cache: true

      - name: Install govulncheck
        run: go install golang.org/x/vuln/cmd/govulncheck@latest

      - name: Run govulncheck
        run: govulncheck ./...

  gosec:
    name: gosec
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run gosec
        uses: securego/gosec@master
        with:
          args: -exclude-dir=test -exclude-dir=tests -exclude-dir=hack -exclude-generated -fmt=sarif -out=gosec.sarif ./...

      - name: Upload SARIF
        uses: github/codeql-action/upload-sarif@v4
        if: always()
        with:
          sarif_file: gosec.sarif
